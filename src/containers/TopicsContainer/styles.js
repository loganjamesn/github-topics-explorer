import styled from "@emotion/styled";

const TopicElementsContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px,1fr));
  margin-top: 1rem;
  margin-bottom: 1rem;
  gap: 1rem;
`;

export { TopicElementsContainer };
